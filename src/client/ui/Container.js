import { ui } from '/client/context';import SignIn from './pages/<SignIn>/SignIn';import SignUp from './pages/<SignUp>/SignUp';import Welcome from './pages/<Welcome>/Welcome';import Forgot from './pages/<Forgot>/Forgot';/* --------------------------------------------------------------------------------------------- */const { Modal, NotFound } = ui.components;export default ui.hoc({  state(props, store) {    return {      currentPage: store.get('currentPage') || props.currentPage,    };  },  render({ state }) {    const { currentPage } = state;    if (currentPage === 'notFound') return ui.html`${NotFound()}`;    return Modal({      currentPage,      pages: {        signin: SignIn,        signup: SignUp,        welcome: Welcome,        forgot: Forgot,      }    });  }});